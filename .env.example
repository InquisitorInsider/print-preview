# Copia este archivo a .env (que NO se sube a git) y define una contraseña
# real. Si ADMIN_PASSWORD queda vacío, el tablero/pantallas/API de
# administración quedan SIN protección en la red local.
ADMIN_USER=admin
ADMIN_PASSWORD=
